{
  "name": "context-kit-packages",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun --filter @context-kit/auth build && bun --filter @context-kit/auth-ui build",
    "typecheck": "bun --filter './packages/*' typecheck",
    "test": "bun --filter './packages/*' test"
  }
}
